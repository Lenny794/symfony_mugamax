{% extends 'base.html.twig' %}

{% block title %}CategoryNews{% endblock %}

{% block body %}
    <h1>CategoryNews</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category_news.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category_news.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('category_news_index') }}">back to list</a>

    <a href="{{ path('category_news_edit', {'id': category_news.id}) }}">edit</a>

    {{ include('category_news/_delete_form.html.twig') }}
{% endblock %}
