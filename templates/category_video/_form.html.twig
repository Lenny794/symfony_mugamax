{{ form_start(form) }}
    {{ form_widget(form) }}
    <button id="signUpButton" class="btn btn-lg btn-primary btn-block signup-btn">{{ button_label|default('Créer une 
                catégorie') }}</button>
{{ form_end(form) }}
