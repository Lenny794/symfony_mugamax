<form method="post" action="{{ path('category_video_delete', {'id': category_video.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category_video.id) }}">
    <button id="signUpButton" class="btn btn-lg btn-primary btn-block signup-btn">Supprimer</button>
</form>
