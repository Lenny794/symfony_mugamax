<form method="post" action="{{ path('category_topic_delete', {'id': category_topic.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category_topic.id) }}">
    <button class="btn">Delete</button>
</form>
