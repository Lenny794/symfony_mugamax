<form method="post" action="{{ path('category_topic_delete', {'id': category_topic.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category_topic.id) }}">
    <button id="signUpButton" class="btn btn-lg btn-primary btn-block signup-btn">Supprimer</button>
</form>
