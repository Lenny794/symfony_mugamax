{% extends 'base.html.twig' %}

{% block title %}CategoryTopic{% endblock %}

{% block body %}
    <h1>CategoryTopic</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category_topic.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category_topic.name }}</td>
            </tr>
            <tr>
                <th>Image_path_url</th>
                <td>{{ category_topic.imagePathUrl }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('category_topic_index') }}">back to list</a>

    <a href="{{ path('category_topic_edit', {'id': category_topic.id}) }}">edit</a>

    {{ include('category_topic/_delete_form.html.twig') }}
{% endblock %}
