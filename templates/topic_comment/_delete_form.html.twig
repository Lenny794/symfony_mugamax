<form method="post" action="{{ path('topic_comment_delete', {'id': topicComment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ topicComment.id) }}">
    <button id="signUpButton" class="btn btn-lg btn-primary btn-block signup-btn">Delete</button>
</form>

